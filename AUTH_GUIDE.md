# دليل نظام الصلاحيات والمستخدمين
## Car Stock Management - Authentication & Authorization System

---

## 📋 نظرة عامة

تم إضافة نظام متكامل للمصادقة والصلاحيات يتيح التحكم الكامل في من يمكنه:
- **عرض** البيانات
- **إضافة** قطع جديدة
- **تعديل** القطع الموجودة
- **حذف** القطع
- **تصدير** البيانات
- **استيراد** البيانات
- **إنشاء نسخ احتياطية**
- **إدارة المستخدمين**

---

## 👥 الأدوار والصلاحيات

### 1. المدير (Admin)
**الصلاحيات الكاملة:**
- ✅ عرض جميع البيانات
- ✅ إضافة قطع جديدة
- ✅ تعديل القطع الموجودة
- ✅ حذف القطع
- ✅ تصدير البيانات
- ✅ استيراد البيانات
- ✅ إنشاء نسخ احتياطية
- ✅ إدارة المستخدمين (إضافة، حذف)

### 2. الموظف (Employee)
**صلاحيات محدودة:**
- ✅ عرض جميع البيانات
- ✅ إضافة قطع جديدة
- ✅ تعديل القطع الموجودة
- ✅ تصدير البيانات
- ❌ حذف القطع
- ❌ استيراد البيانات
- ❌ إنشاء نسخ احتياطية
- ❌ إدارة المستخدمين

### 3. العارض (Viewer)
**عرض فقط:**
- ✅ عرض جميع البيانات
- ❌ إضافة قطع جديدة
- ❌ تعديل القطع الموجودة
- ❌ حذف القطع
- ❌ تصدير البيانات
- ❌ استيراد البيانات
- ❌ إنشاء نسخ احتياطية
- ❌ إدارة المستخدمين

---

## 🔐 المستخدم الافتراضي

عند تشغيل النظام لأول مرة، يتم إنشاء مستخدم مدير افتراضي:

```
اسم المستخدم: admin
كلمة المرور: admin123
الدور: مدير (صلاحيات كاملة)
```

**⚠️ مهم جداً:** قم بتغيير كلمة المرور الافتراضية فوراً بعد أول تسجيل دخول!

---

## 🚀 كيفية الاستخدام

### 1. تسجيل الدخول
1. افتح النظام في المتصفح
2. ستظهر شاشة تسجيل الدخول تلقائياً
3. أدخل اسم المستخدم وكلمة المرور
4. اضغط "تسجيل الدخول"

### 2. إضافة مستخدم جديد (للمدير فقط)
1. سجل دخول كمدير
2. اذهب إلى قسم "المستخدمين" من القائمة الجانبية
3. املأ نموذج إضافة مستخدم:
   - اسم المستخدم (يجب أن يكون فريداً)
   - الاسم الكامل
   - كلمة المرور
   - الدور (مدير / موظف / عارض)
4. اضغط "إضافة مستخدم"

### 3. حذف مستخدم (للمدير فقط)
1. اذهب إلى قسم "المستخدمين"
2. في جدول المستخدمين، اضغط زر "حذف" بجانب المستخدم المراد حذفه
3. أكد عملية الحذف

**ملاحظة:** لا يمكن حذف المستخدم "admin" الرئيسي

### 4. تسجيل الخروج
- اضغط على "تسجيل الخروج" من القائمة الجانبية
- أو سيتم تسجيل الخروج تلقائياً بعد 7 أيام من عدم النشاط

---

## 🔒 الأمان

### الجلسات (Sessions)
- كل جلسة تسجيل دخول صالحة لمدة **7 أيام**
- يتم تخزين الجلسات في ملف `sessions.json`
- يتم حذف الجلسات المنتهية تلقائياً

### كلمات المرور
- يتم تشفير كلمات المرور باستخدام **SHA-256**
- لا يتم تخزين كلمات المرور بشكل نصي
- يتم تخزين البيانات في ملف `users.json`

### التوكنات (Tokens)
- يتم إنشاء توكن فريد لكل جلسة
- التوكن يُرسل مع كل طلب API
- التوكن آمن ومشفر

---

## 📁 الملفات الجديدة

```
car-stock-system/
├── backend/
│   ├── users.json          # بيانات المستخدمين (مشفرة)
│   └── sessions.json       # الجلسات النشطة
└── web-frontend/
    └── auth.js             # نظام المصادقة في الواجهة
```

---

## 🛠️ التخصيص

### إضافة دور جديد
في ملف `backend/main.py`، عدّل قاموس `ROLES`:

```python
ROLES = {
    "admin": {
        "name": "مدير",
        "permissions": ["view", "add", "edit", "delete", "export", "import", "backup"]
    },
    "custom_role": {
        "name": "دور مخصص",
        "permissions": ["view", "add"]  # الصلاحيات المطلوبة
    }
}
```

### تغيير مدة الجلسة
في ملف `backend/main.py`، عدّل دالة `create_session`:

```python
"expires_at": (datetime.now() + timedelta(days=30)).isoformat()  # 30 يوم بدلاً من 7
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: "جلسة غير صالحة أو منتهية"
**الحل:**
- سجل دخول مرة أخرى
- تأكد من صحة اسم المستخدم وكلمة المرور

### مشكلة: "ليس لديك صلاحية"
**الحل:**
- تأكد من أن دورك يسمح بهذا الإجراء
- اتصل بالمدير لتغيير صلاحياتك

### مشكلة: نسيت كلمة المرور
**الحل:**
1. احذف ملف `backend/users.json`
2. أعد تشغيل السيرفر
3. سيتم إنشاء المستخدم الافتراضي `admin/admin123` تلقائياً

---

## 📊 واجهة المستخدم

### التحكم التلقائي في الأزرار
- الأزرار التي تتطلب صلاحيات معينة تُخفى أو تُعطل تلقائياً
- مثال: زر "حذف" يظهر فقط للمدير

### عرض معلومات المستخدم
- يظهر اسم المستخدم ودوره في الشريط الجانبي
- يمكن رؤية الصلاحيات الحالية

---

## 🔄 التكامل مع النظام القديم

النظام الجديد متوافق تماماً مع:
- ✅ جميع البيانات الموجودة
- ✅ نظام التليجرام
- ✅ نظام ImgBB
- ✅ النسخ الاحتياطية
- ✅ الاستيراد والتصدير

---

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي:** احتفظ بنسخة احتياطية من ملف `users.json`
2. **الأمان:** لا تشارك ملف `users.json` أو `sessions.json` مع أحد
3. **كلمات المرور:** استخدم كلمات مرور قوية ومعقدة
4. **الصلاحيات:** امنح الصلاحيات بحذر حسب الحاجة فقط

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الدليل
2. تحقق من ملفات السجلات (logs)
3. تأكد من تشغيل السيرفر بشكل صحيح

---

**تم التطوير بواسطة:** نظام إدارة مخزون السيارات  
**الإصدار:** 5.0.0 مع نظام الصلاحيات  
**التاريخ:** 2025
