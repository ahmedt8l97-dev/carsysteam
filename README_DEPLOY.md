# تعليمات النشر على Render.com 🚀

لقد قمت بتجهيز المشروع ليكون جاهزاً للنشر كخدمة واحدة (Single Web Service) تجمع بين البايثون والـ Vue.

### الإعدادات المطلوبة على Render:

1.  **نوع الخدمة (Service Type)**: اختر **Web Service**.
2.  **لغة البرمجة (Runtime)**: اختر **Python 3**.
3.  **أمر البناء (Build Command)**: 
    ```bash
    ./render-build.sh
    ```
4.  **أمر التشغيل (Start Command)**:
    ```bash
    cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    ```

### المتغيرات البيئية (Environment Variables):
يجب إضافة المتغيرات التالية في قسم **Environment** على Render:

*   `CONVEX_URL`: رابط Convex الخاص بك (موجود في .env.local).
*   `TELEGRAM_BOT_TOKEN`: توكن البوت الخاص بك.
*   `TELEGRAM_CHAT_ID`: رقم الشات/القناة.
*   `IMGBB_API_KEY`: مفتاح API لموقع ImgBB.
*   `PYTHON_VERSION`: يفضل كتابة `3.10.0` أو أحدث.

### ملاحظات تقنية:
*   تم تعديل `backend/main.py` ليقوم بعرض واجهة Vue تلقائياً عند الدخول على الرابط الرئيسي.
*   تمت إضافة أمر بناء تلقائي يقوم بتنصيب مكتبات البايثون وبناء واجهة الـ Vue في خطوة واحدة.
*   النظام يستخدم قاعدة بيانات **Convex السحابية**، لذا ستبقى بياناتك محفوظة حتى عند إعادة تشغيل السيرفر.
