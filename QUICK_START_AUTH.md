# 🚀 البدء السريع - نظام الصلاحيات

## خطوات سريعة للبدء مع نظام الصلاحيات الجديد

---

## 1️⃣ التشغيل الأول

### Windows
```bash
START.bat
```

### Linux/Mac
```bash
# نافذة 1: السيرفر
cd backend
python main.py

# نافذة 2: الواجهة
cd web-frontend
python -m http.server 3000
```

---

## 2️⃣ تسجيل الدخول

افتح المتصفح على: `http://localhost:3000`

**المستخدم الافتراضي:**
```
اسم المستخدم: admin
كلمة المرور: admin123
```

---

## 3️⃣ تغيير كلمة المرور (مهم!)

1. سجل دخول كمدير
2. اذهب إلى "المستخدمين"
3. احذف المستخدم `admin` القديم
4. أضف مستخدم `admin` جديد بكلمة مرور قوية

**أو** احذف ملف `backend/users.json` وأعد تشغيل السيرفر

---

## 4️⃣ إضافة مستخدمين

### للموظفين
```
الدور: موظف
الصلاحيات: عرض، إضافة، تعديل، تصدير
```

### للعارضين
```
الدور: عارض
الصلاحيات: عرض فقط
```

---

## 5️⃣ اختبار الصلاحيات

### كمدير
- ✅ جرب إضافة قطعة جديدة
- ✅ جرب حذف قطعة
- ✅ جرب تصدير البيانات
- ✅ جرب إضافة مستخدم

### كموظف
- ✅ جرب إضافة قطعة (يجب أن يعمل)
- ❌ جرب حذف قطعة (يجب أن يُرفض)
- ❌ جرب إدارة المستخدمين (يجب أن يُخفى)

### كعارض
- ✅ جرب عرض البيانات (يجب أن يعمل)
- ❌ جرب إضافة قطعة (يجب أن يُخفى)

---

## 6️⃣ الأمان

### ✅ افعل
- استخدم كلمات مرور قوية
- غيّر كلمة المرور الافتراضية
- احتفظ بنسخة احتياطية من `users.json`
- امنح الصلاحيات بحذر

### ❌ لا تفعل
- لا تشارك كلمات المرور
- لا تشارك ملف `users.json`
- لا تستخدم كلمات مرور ضعيفة
- لا تمنح صلاحيات غير ضرورية

---

## 🆘 مشاكل شائعة

### نسيت كلمة المرور؟
```bash
# احذف ملف المستخدمين
rm backend/users.json

# أعد تشغيل السيرفر
# سيتم إنشاء admin/admin123 تلقائياً
```

### لا يمكن تسجيل الدخول؟
- تأكد من تشغيل السيرفر
- تأكد من الرابط: `http://localhost:3000`
- جرب المستخدم الافتراضي: `admin/admin123`

### الأزرار معطلة؟
- تحقق من دورك وصلاحياتك
- سجل دخول كمدير للوصول الكامل

---

## 📖 المزيد من المعلومات

- [دليل نظام الصلاحيات الكامل](AUTH_GUIDE.md)
- [سجل التغييرات v5.0](CHANGELOG_v5.md)
- [README الرئيسي](README.md)

---

**نصيحة:** ابدأ بإضافة موظف واحد واختبر الصلاحيات قبل إضافة المزيد!

✅ **جاهز للاستخدام!**
